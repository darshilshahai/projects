# Public site URL, used for metadata, sitemap, and Open Graph tags.
NEXT_PUBLIC_SITE_URL=https://darshilshah.dev

# Optional: Resend API key for the contact form.
# Without it, submissions are logged to the server console and still return success,
# so the form works in development with no setup.
RESEND_API_KEY=

# Where contact form messages are delivered.
CONTACT_TO_EMAIL=darshilshah.ai@gmail.com

# Verified sender for Resend (use onboarding@resend.dev until a domain is verified).
CONTACT_FROM_EMAIL=Portfolio <onboarding@resend.dev>

OPENAI_API_KEY=